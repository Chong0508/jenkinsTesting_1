pipeline {
    agent any

    environment {
        // Defines the subdirectory containing the pom.xml and project files.
        PROJECT_DIR = "CMS" 
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
                echo "Repository structure:"
                sh "ls -R ."
            }
        }

        stage('Verify Project Structure') {
            steps {
                echo "Checking ${PROJECT_DIR}/pom.xml..."
                sh "[ -f ${PROJECT_DIR}/pom.xml ]" 
                // FIX for "mvnw: Permission denied" - ensures the wrapper script is executable.
                sh "chmod +x ${PROJECT_DIR}/mvnw" 
            }
        }

        stage('Initial Docker Build') {
            steps {
                echo "1. Running Maven inside Docker (skip tests)..."
                // Mounts the CMS folder to the container's /app root for simplicity.
                sh """
                    docker run -u 0 --rm \\
                        -v ${WORKSPACE}/${PROJECT_DIR}:/app \\
                        -w /app \\
                        maven:3-jdk-11 mvn clean install -DskipTests=true
                """
            }
        }

        stage('Build & Unit Test') {
            steps {
                echo "2. Running Maven (unit tests) with H2 in-memory database..."
                dir("${PROJECT_DIR}") {
                    // FINAL FIX: Override production database URL for unit tests
                    sh "mvn clean test -B -Dspring.datasource.url=jdbc:h2:mem:testdb"
                }
            }
            post {
                always {
                    junit "${PROJECT_DIR}/target/surefire-reports/*.xml"
                }
            }
        }

        stage('Setup Integration Environment') {
            steps {
                echo "3. Starting Integration environment (docker-compose)..."
                sh """
                    // Assuming you have fixed the 3306 port conflict in docker-compose.yml 
                    docker-compose -f ${PROJECT_DIR}/docker-compose.yml up -d --build
                    echo "Waiting 20 seconds for services to become available..."
                    sleep 20
                """
            }
        }

        stage('Integration Tests') {
            steps {
                echo "4. Running Maven Failsafe (integration tests) against live DB..."
                // The dir() block correctly navigates to the CMS directory.
                dir("${PROJECT_DIR}") {
                    sh "mvn failsafe:integration-test -B"
                }
            }
            post {
                always {
                    junit "${PROJECT_DIR}/target/failsafe-reports/*.xml"
                }
            }
        }
    } // End of stages

    // Post-Build Actions: Cleanup and Reporting
    post {
        always {
            echo "5. Cleaning environment..."
            sh "docker-compose -f ${PROJECT_DIR}/docker-compose.yml down || true"
            cleanWs()
        }
    }
}